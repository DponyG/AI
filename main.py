from node import Node
from node import Tree
from game import Game

def main():
    # root = Node()
    # tree = Tree(root)
    # test = tree.depth(root)
    # tree.miniMax(root, test)
    # tree.printTree(root)
    game = Game(7)

   
    
if __name__ == "__main__":
    main()
  